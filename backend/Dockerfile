# Используем официальный облегчённый образ Node.js на базе Alpine Linux 
FROM node:18-alpine 

# Устанавливаем рабочую директорию внутри контейнера 
WORKDIR /app 

# Копируем только package.json (чтобы закешировать слои с зависимостями) 
COPY package.json ./ 

#мб удалить
COPY . /app

# Устанавливаем зависимости 
RUN npm install 

# Копируем оставшиеся файлы проекта в контейнер 
COPY . . 

# Указываем порт, который будет использовать приложение внутри контейнера 
EXPOSE 3000 

# Команда, запускающая приложение 
CMD ["node", "server.js"]